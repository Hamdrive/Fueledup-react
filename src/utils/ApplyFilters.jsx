

import React from 'react'

export function ApplyFilters(state, action) {
    switch (action.type) {
        case value:
            
            return;
    
        default:
            return state
    }
}
